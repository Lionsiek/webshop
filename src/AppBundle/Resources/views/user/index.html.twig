{% extends 'base.html.twig' %}
         {% block stylesheets%}
         {% endblock %}
    {% block body %}
        <div id="page-wrapper">
            <!-- /.row -->
            <div class="row" style="padding: 30px">
                <div class="col-lg-12">
                    <div class="panel panel-default">
                        <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pb-2 mb-3 border-bottom">
                            <h1 class="h2">Users</h1>
                        </div>
                        <!-- /.panel-heading -->
                        <div class="panel-body">
                            <table width="100%" class="table table-striped table-bordered table-hover" id="dataTables-example">
                                <thead>
                                <tr>
                                <tr>
                                    <th>User ID</th>
                                    <th>First Name</th>
                                    <th>Last name</th>
                                    <th>Role(s)</th>
                                    <th>Last logged in</th>
                                    <th>Actions</th>
                                    <th></th>
                                </tr>
                                </thead>
                                <tbody>
                                {% for user in user %}
                                    <tr>
                                        <td>{{ user.id }}</td>
                                        <td>{{ user.firstName }}</td>
                                        <td>{{ user.lastName }}</td>
                                        {#<td>{% for role in user.roles %}#}
                                                {#{{ role }} &nbsp;&nbsp;#}
                                            {#{% endfor %}</td>#}
                                        <td>{{ user.roles | join (" - ") }}</td>
                                        <td>{{ user.lastLogin | date ("d-m-Y") }}</td>
                                        <td>
                                            <a href="{{ path('role_action_users', { 'id': user.id }) }}">
                                                <button type="button" class="btn btn-primary my-2">{% if user.roles[0] == 'ROLE_SUPER_ADMIN' %}Remove Admin{% else %}Make Admin{% endif %}</button>
                                            </a>
                                        </td>
                                        <td></td>


                                    </tr>
                                {% endfor %}
                                </tbody>
                            </table>

                        </div>
                    </div>
                </div>
            </div>
        </div>
    {% endblock %}
